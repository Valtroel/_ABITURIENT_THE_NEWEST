SET TERM ^ ;

CREATE OR ALTER
    TRIGGER
        ABITUR_LOGBOOK_BI
FOR
    ABITUR_LOGBOOK
ACTIVE
    BEFORE INSERT
        POSITION 0
AS
BEGIN
    IF (NEW.ID_LOG IS NULL)
    THEN
        NEW.ID_LOG = GEN_ID(GEN_ABITUR_LOGBOOK_ID, 1);
END
^

SET TERM ; ^
